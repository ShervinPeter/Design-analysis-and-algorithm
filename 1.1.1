#include <stdio.h>
 void read1(int arr[], int n){
	 printf("Enter %d elements : ", n);
	int i;
	 for (i = 0; i < n; i++){
		 scanf("%d", &arr[i]);
	}
}

int linearSearch(int arr[], int i, int size,int key){
	if (i > size)
		return -1;
	if (arr[i] == key)
		return i;
	return linearSearch(arr, i+1, size, key);
}
